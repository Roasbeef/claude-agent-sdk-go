# Integration Followups

- Backfill `TestIntegrationStopHookBackgroundTasks` when the CLI test fixture can deterministically create a long-running background task.
- Backfill `TestIntegrationStopHookSessionCrons` when the CLI test fixture can deterministically create a session cron.
- Backfill `TestIntegrationHookTerminalSequence` when the CLI test fixture can capture terminal-escape bytes emitted by hook `terminalSequence` returns. Currently those go straight to the controlling TTY and aren't observable on the SDK transport.
- Backfill `TestIntegrationHookSuppressOriginalPrompt` when the CLI test fixture can assert the block-message body the CLI returns when a UserPromptSubmit hook returns `suppressOriginalPrompt: true`. The omission is a CLI rendering behavior, not surfaced on the SDK transport.
- Backfill `TestIntegrationSessionStartReloadSkills` when the CLI test fixture can change `CLAUDE_SKILLS_DIR` between SessionStart and the first message so reload-skills behavior is observable in standard integration runs.
- Backfill `TestIntegrationSessionTitleField` when the CLI test fixture can explicitly rename the session so `session_title` is emitted on SessionStart and UserPromptSubmit hook inputs.
- Backfill `TestIntegrationPostToolUseUpdatedToolOutput` when the CLI test fixture can deterministically run a tool whose output a PostToolUse hook rewrites, so the model receives the rewritten value rather than the original tool response.
- Backfill `TestIntegrationAssistantMessageSubagentFields` when the CLI test fixture can deterministically run a subagent task end-to-end.
- Backfill `TestIntegrationUserMessageSubagentFields` when the CLI test fixture can deterministically run a Task-tool subagent whose tool-result user message round-trips `subagent_type` + `task_description`.
- Backfill `TestIntegrationAssistantMessageError` when the CLI test fixture can deterministically force an upstream API failure into an `assistant` event.
- Backfill `TestIntegrationResultMessageOriginTTFT` when the CLI test fixture can deterministically emit `origin` and/or `ttft_ms` on a result event.
- Backfill `TestIntegrationResultMessageTimingFields` when the CLI test fixture can deterministically emit spawn-pool timing instrumentation (`ttft_stream_ms`, `time_to_request_ms`, `time_to_request_from_spawn_ms`, `warm_spare_claimed`) on a result event.
- Backfill `TestIntegrationTaskLifecycleFields` when the CLI test fixture can deterministically run a Task-tool subagent (populates `subagent_type` on `task_started`/`task_progress`) and pause a running task (emits `task_updated` with `status:"paused"`).
- Backfill `TestIntegrationThinkingTokensMessage` when the CLI test fixture can deterministically invoke a thinking-capable model through a redacted-thinking phase so `thinking_tokens` system events are emitted.
- Backfill `TestIntegrationCompactBoundaryPreservedMessages` when the CLI test fixture can deterministically trigger a partial compaction with messagesToKeep so the `compact_metadata.preserved_messages` block is populated.
- Backfill `TestIntegrationMemoryRecallOrganizationScope` when the CLI test fixture can deterministically surface an organization-scoped memory (https URL + inline content) on a `memory_recall` system event.
- Backfill `TestIntegrationPartialAssistantParentToolUseID` when the CLI test fixture can deterministically emit a streaming partial event nested under a Task-tool subagent so `parent_tool_use_id` carries a non-null tool_use id.
- Backfill `TestIntegrationHostAuthTokenRefresh` when a real auth-failure path or a CLI-side fixture flag can deterministically trigger the `host_auth_token_refresh` control request. Currently not triggerable from the CLI in standard integration runs; verified via unit test.
- Backfill `TestIntegrationSettingsManagedOrgFields` when the CLI test fixture can deterministically inject a managed-settings.json / MDM policy tier so the SDK can observe the v0.3.150/v0.3.168 managed-org additions (`policyHelper`, `parentSettingsBehavior`, `claudeMd`, `disableAgentView`, `disableRemoteControl`, `allowAllClaudeAiMcps`, `isolatePeerMachines`, `daemonColdStart`, `disableDeepLinkRegistration`, `defaultView`, `requiredMinimumVersion`, `requiredMaximumVersion`, `switchModelsOnFlag`, `forceLoginMethod:"gateway"`) round-tripping through the resolved-settings transport. These fields are honored only from admin-controlled policy sources.
- Backfill `TestIntegrationSettingsSandboxFields` when the CLI test fixture can deterministically inject the v0.3.150 sandbox additions (`sandbox.network.tlsTerminate`, `sandbox.bwrapPath`, `sandbox.socatPath`) via managed-settings.json so the SDK can observe them round-tripping through the resolved-settings transport. `tlsTerminate` is experimental runtime sandbox behavior, and `bwrapPath` / `socatPath` are honored only from admin-controlled managed settings.
- Backfill `TestIntegrationSettingsWorktreeFields` when the CLI test fixture can deterministically inject the v0.3.150 worktree additions (`worktree.baseRef`, `worktree.bgIsolation`) via managed-settings.json so the SDK can observe them round-tripping through the resolved-settings transport. Both fields drive the CLI's `--worktree` / `EnterWorktree` flow which the Go SDK does not invoke directly.
- Backfill `TestIntegrationSettingsMiscFields` when the CLI test fixture can deterministically inject the v0.3.150 misc additions (`statusLine.hideVimModeIndicator`, marketplace source variants `skills-dir` / `unsupported`) via managed-settings.json so the SDK can observe them round-tripping through the resolved-settings transport. Both surfaces drive CLI code paths (statusline renderer, plugin marketplace registration) the Go SDK does not invoke directly.
- Backfill `TestIntegrationCommandsChangedMessage` when the CLI test fixture can deterministically dynamically discover a new skill mid-session (e.g. by writing a SKILL.md into `CLAUDE_SKILLS_DIR` between session start and a later message) so the `commands_changed` system event is emitted with the refreshed slash-command list.
- Backfill `TestIntegrationStopHookAdditionalContext` when the CLI test fixture can deterministically observe the model consuming `hookSpecificOutput.additionalContext` from Stop / SubagentStop and continuing the turn. The side effect is a model-side decision not assertable on the SDK transport in standard runs.
- Backfill `TestIntegrationUserDialogCallback` when the CLI test fixture can deterministically fire a `request_user_dialog` control request (currently driven only from CLI-side tools that invoke `setToolJSX` with an `onDone` callback, not surfaced through the standard integration run). Verified via unit tests in `protocol_test.go`.
- Backfill `TestIntegrationStreamRegisterRepoRoot` when the CLI surfaces a handler for the `register_repo_root` control request. As of v0.3.168 the TS SDK declares `SDKControlRegisterRepoRootRequest` in `sdk.d.ts` but `sdk.mjs` ships no dispatcher, so the request is not deterministically exercisable end-to-end. Verified via unit tests in `stream_control_test.go` and `messages_test.go`.
- Backfill `TestIntegrationResumeDropsTurn` when the installed CLI implements `--resume-drops-turn`. The 2.1.222 binary this suite runs against does not carry the flag (its `--resume-session-at` pair is present), so a live run would fail on an unknown option instead of exercising the fork-point guard. Verified via argv assertions in `transport_test.go`.
- Backfill `TestIntegrationPerTaskStopAffordance` when the CLI test fixture can deterministically start a long-running background agent/workflow task and issue an interrupt, so the v0.3.251 `perTaskStopAffordance` declaration can be observed selecting between "spare background tasks" and the fail-closed "kill them" default. The current test only proves the CLI accepts the initialize field.
- Backfill `TestIntegrationModelSwitchHooks` when the installed CLI emits `PreModelSwitch` / `PostModelSwitch` hook events. The 2.1.222 binary this suite runs against fires neither for a `set_model` control request, so the test skips rather than asserting on a switch that never reports.
- Backfill the first-reply-frame half of `TestIntegrationUserMessageUUIDCorrelation` when the installed CLI stamps `user_message_uuid` on the turn's first assistant message / first non-ping stream event. CLI 2.1.222 echoes it on the result only, so the test asserts the result echo (live) and skips the rest.
- Backfill `TestIntegrationQueuedTurnCount` when the installed CLI reports `queued_turn_count` on results. CLI 2.1.222 omits it even with a second send queued behind the first, so the test builds a real backlog and then skips rather than asserting on an absent field.
- Backfill `TestIntegrationAmbientTaskMarker` when the installed CLI stamps `ambient` on task frames. Probed against CLI 2.1.222 with a prompt that spawns an Explore subagent: `task_started` carries neither `ambient` nor `skip_transcript`, so the test spawns a real task and then skips rather than re-asserting an absent field.
- Backfill `TestIntegrationSessionStartResumeCost` when the installed CLI fires SessionStart hooks to SDK transports on a `--resume`. The test builds a real transcript and resumes it successfully (note: it cannot use `isolatedClientOptions`, which disables session persistence and so makes resume impossible), but CLI 2.1.222 delivers no SessionStart hook callback, so the v0.3.251 resume-cost fields — `seconds_since_last_response`, `context_tokens`, `prompt_cache_likely_expired`, `estimated_cache_write_usd` — cannot be observed.
